#include <stdio.h>
#include <string.h>

int main()
{
    char string1[] = "Julian";
    char string2[] = "Ehrmaier";

    //strlwr(string1);                              //converts everything lowercase
    //strupr(string1);                              //converts everything uppercase
    //strcat(string1, string2);                     //appends str2 to end of str1
    //strncat(string1, string2, 1);                 //appends n characters from str2 to str1
    //strcpy(string1, string2);                     //copy str2 to str1
    //strncpy(string1, string2, 1);                 //copy n characters of str2 to str1

    //strset(string1, '?');                         //sets all characters of a string to given character
    //strnset(string1, 'x', 1);                     //sets first n characters of a string to a given character
    //strrev(string1);                              //reverses a string

    //printf("%s", string1);



    //int result = strlen(string1);                 //returns string length as int
    //int result = strcmp(string1, string2);        //string compare all characters 
    //int result = strncmp(string1, string2, 1);    //string compare n characters
    //int result = strcmpi(string1, string2);       //string compare all (ignore case)
    //int result = stricmp(string1, string2, 1);    //string compare n characters (ignore case)

    //printf("%d", result);


    /*
    if(result == 0)
    {
        printf("These strings are the same");
    }
    else
    {
        printf("These strings are not the same");
    }
    */

    return 0;
}